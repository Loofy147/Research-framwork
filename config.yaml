experiments:
  - name: example-adversarial
    agents:
      - variant: BasicParserAgent
        params:
          temperature: 0.7
      - variant: my-agent-v1
    rounds: 10
    metrics:
      - success_rate
      - avg_latency